const { postings, types, subtitles, contents } = require('./access');

import {
  CompanyRecord,
  CompanyServiceType,
  TypeRecord,
  SubtitleRecord,
  ContentRecord,
} from '../interfaces';

const CompanyService: CompanyServiceType = {};
